name: Assignment Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 25ds2000008@ds.study.iitm.ac.in
        run: echo "Hello, world!"
